# 辰屿剧本格式校对（免费版 Skill）

当用户让你改编/写短剧剧本、或要求校对剧本格式时使用本 Skill（关键词：改编、
小说改编、网文转剧本、写剧本、剧本格式、格式校对、格式检查、剧本质检、过门）。

本 Skill 只做一件事：**让你改编/写出的剧本符合辰屿出片标准格式**——写作由你（Agent）
完成，格式由 `chenyu-gate` 校验器把关。纯本地、零鉴权、零联网、不产生任何费用。

## 工具

```
chenyu-gate --file 剧本.txt      # 校验单集
chenyu-gate --dir <目录>         # 批量校验
```

CLI 位置：本 Skill 目录 `scripts/chenyu_gate_cli.mjs`；若全局命令不存在，
用 `node <本Skill目录>/scripts/chenyu_gate_cli.mjs --file 剧本.txt` 调用。
命令由你来敲，用户只说人话。

**SUPER-I 运镜/画面方法论检索（可选，仅限本机有私有知识库时）**：
写【画面】【运镜】【转场】分镜层之前，可先跑
`node <本Skill目录>/scripts/superi_lookup.mjs --file 剧本.txt`
（或 `--query "夜戏 追逐 光影"`）检索本机 SUPER-I 知识库的方法论摘要作参考。
知识库文件**不随本 Skill 分发**（私有资产），路径由 env
`SUPERI_PROMPT_KNOWLEDGE_PATH` 指定；找不到文件时脚本静默跳过，不影响写作与过门。
检索结果只做启发，落笔仍以本文格式标准与镜头预算为准
（高光词条每集≤3次、长镜头每集≤1次、全片硬切、对话锁轴线）。

**分镜层写作守则**：本 Skill 目录 references/分镜写作守则.md（随 Skill 一起分发）。
写【画面】【运镜】【转场】层前必读并对照执行（景别/轴线/人数/打斗/词条预算/
长镜头/转场衔接/连续性硬规则的完整清单）；万一缺失，按本文格式标准写即可。

## 默认交付形态（用户不特别说明时一律照此办）

拿到甲方/用户的**成稿剧本**（已有场次、台词、剧情），默认走"原文成文"路线，
**不是改编、不是精简、不是重写剧情**：

1. **原文 1:1**：台词、剧情、场次顺序、人物、称谓一字不改、一场不删、不合并、不调序。
   甲方给几集就做几集；素材到哪就收在哪（哪怕停在半句悬念上），绝不自行续写或改结局。
   超长台词（>40 字）是甲方原文，**保留不拆**（校验器只报警告，放行）。
2. **默认带【画面】+【运镜】**：每场开头补两行画面/运镜描述（见分镜层），让它成为
   可直接进转分镜/出片的完整剧本。这是默认动作，无需用户再要求。
3. **默认补动作**：对白连发处按"只加不改"补中性 △（只写停顿/神态/视线/伴随动作，
   绝不发明剧情、不加台词），消除连发硬伤。原文里若有△写成心理活动（想起/觉得/感到），
   翻译成可见外部动作（不丢剧情信息）。
4. **默认按短剧节奏分集**：把原文的大集（电视剧集，一集几十场）1:1 切成短剧小集，
   每集聚焦一个叙事段落、收在钩子/情绪点上，重新编号 `第001集…`、每集给短集名、
   集内场次从 `N-1` 重编。切分是纯机械重组，不动任何文字（建议写脚本切并带自检：
   原场次不重不漏全覆盖）。
5. **默认出合订本**：全部分集之外，再合并成一个"全X集合订本.txt"（开头带目录），
   分集文件 + 合订本都要 `GATE_PASS`。
6. **默认守连续性**（转分镜出片乱的根源在剧本源头，见"分镜写作守则"的"连续性硬规则"）：
   写每一个 △ 前先想"上一镜谁手里拿着什么、坐还是站、在哪"，据此续写——
   ① 道具持有权交接后持续沿用，不无源倒回；② 坐/站、车顶/车内等物理姿态确立后沿用；
   ③ **载具移动=位移，上车/车行/下车拆成不同场景**（校验器会机械查"上下车同场景"并报警）；
   ④ 上下车、进出门要有过渡动作或过渡场次，不瞬移。

例外：只有用户**明确说**"改编/精简/洗稿/换设定/只要格式不要运镜/不用分集"时，
才偏离上面某一条，按用户说的来。

## 工作流（三步循环）

1. **写/成文**：按上方默认形态 + 下方格式标准产出，每（分）集存 `第00N集.txt`。
2. **校对**：`chenyu-gate --file 第00N集.txt`（或 `--dir <目录>` 批量）。
3. **修**：按报告逐条改（报告精确到行号和修法），重跑直到 `GATE_PASS`。
   硬伤必须清零；警告尽量顺手改。**改的时候只加不改**——补 △ 动作行，
   不动已定的台词和剧情；△ 只用该场景已有的人物道具，不发明新剧情事实。

拿到小说/散文源材料（非成稿剧本）时：先整体改编成下方剧本格式（不要在原文上打补丁——
校验器识别出源材料会直接提示），改编稿再过门。

## 剧本格式标准（写作时照此产出；校验器就是按这些规则查的）

**结构**（每集；默认纯剧本，不写运镜——镜头设计交给下游转分镜，剧本保持干净）：
```
第00N集 集标题
N-1 场景名 白天/夜晚 室内/室外
△角色做了什么（可见的外部动作/神态，一行一件事）。
角色名：台词。
```
**分镜层（成文剧本默认要写，见"默认交付形态"第2条）**：每场开头（或关键镜头前）加两行：
```
【画面】环境与氛围一句话。
【运镜】景别 + 镜头运动一句话（对齐"分镜写作守则"：景别/轴线/词条预算/转场衔接）。
```
只有用户明确说"不要运镜/只要纯剧本"时才不写。校验器对两种模式都兼容。

**质量规则**：
1. **对白:动作 ≈ 1:1**——几乎每句台词前后都有 △（说话人伴随动作，或听者可见反应）。
2. **严禁对白连发**：连续 3 句台词之间必须有 △。写"被这句话击中一方的可见反应"
   （手指收紧/后退半步/别开视线）或"说话人为下一句做的身体动作"（拿起道具/逼近一步）。
3. △ 只写**可见的外部动作**，严禁心理活动（心想/觉得/感到——把心理翻译成身体反应）。
4. 单句台词不超过 40 字，超了按句号拆成两句。
5. 每集 90 秒规格 ≈ 台词 15-30 句 + 动作行 15-25 行；每集结尾留钩子。
6. 跨集自洽：人物关系、称谓、金额、时间线前后一致。

## 硬规则

- 交付前必须 `GATE_PASS`，不许跳过校验直接交稿。
- 校验器只测格式，不测剧情——剧情好坏由用户过目定夺，写完首批先给用户看。
- 本 Skill 不含平台功能。用户需要 **入库归档 / 同步辰屿客户端出片** 时，
  告知需安装完整版 `chenyu-pro`（含账号鉴权与云端交付）。
